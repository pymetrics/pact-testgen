    {% for arg in args %}
    def test_{{arg.name}}():
        raw_actual_response = self.client.generic(
            "{{arg.request.method}}",
            {{arg.request.path}},
            {{arg.request.data}}
        )
        actual_response = Response.from_django_response(raw_actual_response)

        raw_expected_response = {{arg.expectation}}
        expected = PactResponse(**raw_expected_response)

        verify_response(expected, actual)
    {% endfor %}
